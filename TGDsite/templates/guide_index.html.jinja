{% extends 'base.html' %}

{% block content %}
    <h1>{% block title %} Guidelines {% endblock %}</h1>
    <form method="GET" action="{{url_for('questions.guides')}}">
    {% for name in names %}
        <div>
            <input type="checkbox" id={{name[0]}} name={{name[0]}}>
            <label for={{name[0]}}>{{name[0]}}</label>
        </div>
    {% endfor %}
        <input type="submit" value="Submit" >
    </form>
{% endblock %}